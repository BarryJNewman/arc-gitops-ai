apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NOTE: Do not use this file directly — use overlays/nvidia or overlays/rocm instead.
# This file exists for backward compatibility only and defaults to the rocm overlay.
resources:
  - overlays/rocm
